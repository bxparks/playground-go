name: Go Verify

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: ghcr.io/tinygo-org/tinygo

    steps:
    - name: Checkout Playground Go
      uses: actions/checkout@v3

    - name: Build
      run: |
        make build buildtiny

    - name: Tests
      run: |
        make test testtiny
